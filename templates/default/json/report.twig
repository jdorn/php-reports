[{% for row in DataSets[0].rows %}{% if not loop.first %},{% endif %}

	{{'{'}}{% for value in row.values %}{% if not loop.first %},{% endif %}

		"{{value.key}}":"{{ value.getValue()|replace({'\\':'\\\\','"':'\\"' })|raw }}"{% endfor %}

	}{% endfor %}

]
