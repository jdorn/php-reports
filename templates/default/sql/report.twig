INSERT INTO `{{Name}}`
	({% for value in Rows[0].values %}{% if not loop.first %},{% endif %}`{{value.key}}`{% endfor %})
VALUES
{% for row in Rows %}{% if not loop.first %},
{% endif %}	({% for value in row.values %}{% if not loop.first %},{% endif %}
"{{ value.getValue()|replace({'\\':'\\\\','"':'\\"' })|raw }}"{% endfor %}){% endfor %};
